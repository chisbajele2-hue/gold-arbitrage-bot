name: Keepalive

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: |
          curl -sf https://gold-arbitrage-bot.onrender.com/health || echo "ping failed (free tier may be sleeping)"